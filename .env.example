# ── Environment Variables Template ──
# Copy this file to .env locally. DO NOT commit .env to GitHub.

# Application Environment (development, staging, production)
APP_ENV=production

# Database Connection String (If extending to a remote DB like Supabase/Firebase)
DATABASE_URL=

# Sample external API keys (e.g., for future analytics or auth)
API_KEYS=
